# Binance Email Checker API
> Official NumberChecker integration examples for asynchronous bulk Binance verification.

## Canonical resources
- [Product page](https://numberchecker.ai/products/binance-email-checker)
- [API documentation](https://docs.numberchecker.ai/binance-email-checker)
- [GitHub repository](https://github.com/numberchecker-ai/binance-email-checker-api)
- [Dashboard](https://platform.numberchecker.ai)
- [Pricing](https://numberchecker.ai/pricing)

## Product facts
- Brand: NumberChecker.
- Product code: `binance_email` only.
- API base URL: https://api.numberchecker.ai.
- Authentication: X-API-Key request header.
- Input: 2,000–10,000,000 email values in a text file, one per line.
- Processing: asynchronous bulk task.
- Export fields: `email`, `activated`.
- Examples: Python, Node.js, Go, Java, C#, PHP, JavaScript, Shell.

## Workflow
1. Send multipart `POST /v1/tasks` with `file` and `task_type=binance_email`.
2. Send multipart `POST /v1/gettasks` with `task_id` until `status=exported` or `failed`.
3. Download `result_url` only after export.
4. Use `GET /v1/balance` to retrieve the current account balance.

## Scope and security
- This repository covers only `binance_email`.
- Read the API key from the `NUMBERCHECKER_API_KEY` environment variable.
- Never expose an API key in production browser code.
- Process only identifiers you are authorized to verify.

Last reviewed: 2026-08-18.
